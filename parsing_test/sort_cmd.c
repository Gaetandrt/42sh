/*
** EPITECH PROJECT, 2022
** sort the cmd with priority
** File description:
** sort_cmd
*/

#include "my.h"

char **sort_cmd(char *cmd, sep_node **head, mysh_t *mysh)
{
    char **sorted_cmd = malloc(sizeof(char *) * (count_sep(cmd) + 3));
    char *tmp;

}
